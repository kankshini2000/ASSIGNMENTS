class Rectangle {
    constructor(len, width) {
      this.len = len;
      this.width = width;
    }
  }
  
  const obj = new Rectangle(5, 8);
  document.write("the len is : "+obj.len+ "and width of rect is:"+obj.width); 
